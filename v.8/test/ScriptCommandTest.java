/**
 * Tests for file input on script command.
 */
public class ScriptCommandTest {
  //test with one script command submitted (and that program exits after running the script)
  //test that interactive commands work when no script present
  //test that can call a txt script command after having manually run certain commands

  //test that txt file with script matches manually inputted scripted
}
